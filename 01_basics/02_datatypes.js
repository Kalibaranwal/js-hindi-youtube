"use strict"; // treat all JS code as a newer version of js

// alert[3+3] // we are using node js ,not browser

console.log(3
    +3) // code readability should be high but here value give 6

    let name="Kali"
    let age=21
    let isLoggedIn=true
    let state;

    // number => 2 to the power 53
    // bigInt
    // string => ""
    //boolean => true/false
    //null => standalone value
    //undefined => value not assign
    //symbol => used for uniqueness

    //object

    console.log(typeof "Kali");  // type is string
    console.log(typeof null);  // null type is object
    console.log(typeof undefined); // undefined type is undefined
